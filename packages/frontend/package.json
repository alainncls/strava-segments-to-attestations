{
  "name": "@strava-attestations/frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "clean": "rm -rf dist coverage",
    "dev": "vite",
    "prebuild": "pnpm --filter @strava-attestations/shared build",
    "pretest": "pnpm --filter @strava-attestations/shared build",
    "pretest:coverage": "pnpm --filter @strava-attestations/shared build",
    "preview": "vite preview",
    "test": "vitest run",
    "pretest:e2e": "pnpm build",
    "preview:e2e": "vite preview --host 127.0.0.1 --port 4177",
    "test:e2e": "playwright test",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@reown/appkit": "catalog:",
    "@reown/appkit-adapter-wagmi": "catalog:",
    "@strava-attestations/shared": "workspace:*",
    "@tanstack/react-query": "catalog:",
    "@wagmi/connectors": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-router-dom": "catalog:",
    "viem": "catalog:",
    "wagmi": "catalog:"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "@testing-library/jest-dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-react-hooks": "catalog:",
    "jsdom": "^29.1.1",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
